//Arrow Functions or Fat arrow functions
//no name, always expression, assign to variable
//"this"
/*
const sayHi = () => console.log("hello");
sayHi();

//um parâmetro e uma linha, pode omitir o parênteses do parâmentro, e as chaves da função. Também não precisa usar o return pos está implícito
const double = (value) => value * 2;
const num = double(4);
console.log(num);

//two parameters and more than one line. Tem que usar () nos parâmetros e {} na função. Tem que usar o return
const multiply = (num1, num2) => {
  const result = num1 * num2;
  //more code here
  return result;
};

const sum = multiply(5, 4);
console.log(sum);

//return object
//precisa adicionar () antes do objeto para não dar erro

const object = () => ({ name: "caroline", age: 27 });
const person = object();
console.log(person);

// arrow como callback function
//novo array feito com arrow function somente números maiores que 2
const numbers = [1, 2, 3, 4, 5, 6];
const big = numbers.filter(number => number > 2);
console.log(big);

//addEventListener
const btn = document.querySelector(".btn")
btn.addEventListener("click",() => console.log("you clicked me"));*/

